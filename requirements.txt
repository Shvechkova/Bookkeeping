# Основные зависимости Django
django==5.2
django-environ

# База данных (выберите одну):
# Для SQLite (рекомендуется для передачи приложения):
# (SQLite встроен в Python, дополнительные пакеты не нужны)

# Для MySQL (production):
# pymysql

# Django REST Framework
djangorestframework==3.16.0

# WSGI сервер для production
waitress

# Дополнительные зависимости для сборки
setuptools
wheel
